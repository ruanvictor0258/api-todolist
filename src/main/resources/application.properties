# --- Configura��o do Banco de Dados PostgreSQL ---
# L� a URL, o usu�rio e a senha das vari�veis de ambiente do Render
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASS}

# --- Configura��o do Hibernate (JPA) ---

# Informa ao Hibernate qual "dialeto" SQL ele deve usar para o Postgre
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# IMPORTANTE: 'update' tenta atualizar as tabelas se voc� mudar suas @Entities.
# N�o use 'create' ou 'create-drop' em produ��o, pois apaga seus dados.
spring.jpa.hibernate.ddl-auto=update

